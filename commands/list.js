module.exports = {
    name: 'list',
    description: 'this command lists all the commands this bot has to offer',
    execute(message, args) {
        message.channel.send('still just testing this shit')
    }
}